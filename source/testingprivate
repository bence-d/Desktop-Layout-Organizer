import shutil
import dlo_library as dlo

#filepath = "C:\\Users\\hvb63\\Desktop\\testmove1.txt" 
#registryDirectory = "C:\\Users\\hvb63\\Desktop\\testfolder\\testmove1.txt"
#desktop_path = os.path.join(os.path.expanduser('~'), 'Desktop')

#dlo.presetmanager.create_preset("presetTest", "presetDescription")

#shutil.move(filepath,registryDirectory)

#move  file to  folder
#os.rename(filepath,registryDirectory)



shortcut_path = "C:\\Users\\hvb63\\Desktop\\testtest123.lnk"
import win32com.client # pip install pywin32

def return_shortcut_target(shortcut_path):
    shell = win32com.client.Dispatch("WScript.Shell")
    shortcut = shell.CreateShortCut(shortcut_path)
    return shortcut.Targetpath


